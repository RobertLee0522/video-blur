# P0 測試報告

- 受測版本: `main` @ `2fdb4f0` (未修改 `app.py` / `tracker.py`)
- 測試計畫: [TEST_PLAN.md](TEST_PLAN.md)
- 測試腳本: `tests/p0_*.py`, 原始數據: `tests/reports/*.json` (逐幀 CSV 與測試影片會產生在 `tests/out/`, 不進版控)

## 測試環境

| 項目 | 值 |
|---|---|
| 機器 | Windows 11 Enterprise, x86-64, NVIDIA GPU (NVENC 可用) |
| Python / OpenCV / NumPy | 3.11.13 / 4.10.0 / conda env `311` |
| ffmpeg | 4.3.1 (chocolatey) |
| 追蹤參數 (未特別註明時) | TR-960, min_conf 0.3, hold_frames 5, padding 5% |

> ⚠️ **不是 M5 Mac.** 本報告沒有任何 VideoToolbox 數據; P0 的邏輯正確性與平台無關, 但效能數字不能直接套用到 M5.
> 重跑方式: `cd tests` 後依序執行 `python p0_1_similar.py`、`p0_2_lost.py`、`p0_3_cache.py`、`p0_4_output.py`、`p0_5_geometry.py` (需 ffmpeg / ffprobe 在 PATH).

---

## P0-1 相似窗戶誤鎖

- **測試影片**: 合成 1920×1080, 30 fps, 420 幀, H.264 CRF12. 相機停在 A → f45~105 快速平移到 B (含動態模糊與透視) → 停在 B → f225~285 平移回 A.
  - **V1-sim**: 同款窗框, 窗內內容不同, 周圍牆面不同
  - **V1-twin**: 同型號窗戶、同樣窗簾 (窗戶本體像素相同), 周圍牆面不同 ← 公寓大樓最常見
  - **V1-ident**: A 與周圍 400 px 完全複製成 B (外觀無法區分, 已知極限)
- **操作**: 第 0 幀用真值四角框選 A, 逐幀追蹤; `min_conf` 分別 0 / 0.3 / 0.5 / 0.7.
- **事件幀號 (三種影片相同)**: A 開始離開 f68, A 完全消失 f74, B 入鏡 f77, B 完整可見 f83, A 回到 ≥50% f260.

| 影片 | min_conf | 誤鎖幀數 (幀號) | 找回延遲 | 離開時狀態序列 | 判定 |
|---|---|---|---|---|---|
| V1-sim | 0 / 0.3 / 0.5 / 0.7 | **0** | 0 幀 | HOLD×5 → LOST_STOPPED ✓ | **Pass** |
| V1-twin | 0.3 (預設) | **174** (f80–253) | 0 幀 (f260) | HOLD×5 → LOST_STOPPED → f80 起鎖到 B | **Fail** |
| V1-twin | 0.7 | **173** (f81–253) | 1 幀 | 同上 | **Fail** |
| V1-ident | 0.3 / 0.7 | 175 / 173 (f79–253) | 0 幀 | HOLD×5 → 鎖到 B | 已知極限, 只記錄 |

**可信度分布** (NCC, 越高越像框選當下的樣板)

| 影片 | 追蹤 A 正確時 (min / p5 / 中位數) | B 對 A 樣板 (min / 中位數) | 鎖在 B 時的 conf |
|---|---|---|---|
| V1-sim | 0.727 / 0.942 / 0.983 | 0.567 / 0.653 | (未發生) |
| V1-twin | 0.615 / 0.947 / 0.982 | **0.866 / 0.957** | min 0.621, 中位數 0.938 |
| V1-ident | 0.704 / 0.945 / 0.990 | 0.902 / 0.996 | 0.48 → 0.86 |

**實際結果**
1. **ORB/NCC 找回會不會把 B 當成 A**: sim 不會 (ORB 只 2 次比對到 B 附近, 且位置錯, NCC 0.01~0.07 被拒絕). **twin 會**: ORB 比對到 B 39 次, NCC 中位數 0.954, 通過 `max(0.5, min_conf)` 門檻.
2. **回到 A 能否正確恢復**: 三種影片都能. B 離開、A 回來時立即切回 A (延遲 0~1 幀), 之後 30 幀 Leak = 0, IoU ≥ 0.8.
3. **誤鎖時是否持續模糊**: 是. twin 的 f80–253 模糊套在 B 上 (這段 A 不在畫面內, 所以 A 沒有外洩, 但 B 被錯誤模糊約 5.8 秒).
4. **confidence 是否明顯下降**: **不會**. 鎖在 B 時 conf 中位數 0.938, 與追蹤 A 正確時 (0.982) 幾乎相同.
5. **hold_frames / min_conf 能否避免**: hold_frames 正常 (f74–78 HOLD, f79 停止模糊), 但 **min_conf 無效** — 在 twin 上 B 的 NCC 最低 0.866, 高於任何合理門檻; 若門檻調到 0.87 以上, 追蹤 A 正確時 (p5 = 0.947, min = 0.615) 也會開始被誤判遺失.
6. **UI 顯示**: 誤鎖期間狀態為 TRACKING (綠框), 沒有任何警示.
7. **sim 的 conf 區分力**: 正確追蹤的 p5 (0.942) > B 對樣板的最大值 (0.657), 可區分. 但 sim 裡 B 的 0.57~0.66 仍 **高於找回門檻 0.5**; 實際擋下 B 的是 ORB 特徵比對失敗, 不是 NCC 門檻.

**關於「找回門檻提高到 0.5 是否降低誤認」**: 門檻 0.5 對 sim 沒有發揮作用 (B 的 NCC 本來就 > 0.5), 對 twin 也擋不住 (0.87~0.96). 目前能避免誤認的只有「窗戶本體外觀不同」這個條件.

- **是否需要修改**: 是 (見 🔧 #1).
- **效能**: 追蹤 21~63 ms/幀 (TR-960, 同時有其他測試在跑, 僅供參考).

---

## P0-2 遺失 → HOLD → LOST_STOPPED

- **測試影片**: V6-occlude, 合成 1920×1080, 30 fps, 520 幀, 只有窗戶 A, 相機慢速漂移.
- **操作**: 逐幀追蹤並實際呼叫 `engine.render()` 繪製, 比對繪製前後**每個像素**; hold_frames 分別 0 / 5 / 15. UI 部分啟動真正的 App, 在 L+2 / L+6 讀取畫布物件與清單狀態.

| 情境 | 幀號 | 預期 | 實際 (hold=5) | 判定 |
|---|---|---|---|---|
| A. 甩鏡離開 3 幀 | f470–472 | HOLD 並持續模糊, 回來 ≤15 幀恢復 | f470–472 HOLD, 3 幀都有模糊; f473 恢復 TRACKING (延遲 0) | **Pass** |
| B. 遮擋 50%, 3 幀 | f60–62 | TRACKING 或 HOLD, 未遮擋部分 Leak ≤ 5% | 全程 TRACKING, Leak = 0 | **Pass** |
| C. 遮擋 100%, 30 幀 | f120–149 | L..L+4 HOLD, L+5 起 LOST_STOPPED 且不模糊 | L = 120: f120–124 HOLD, **f125–149 LOST_STOPPED, 改動像素 = 0**; 沒有追到遮擋物上 | **Pass** |
| C. 平移離開約 126 幀 | f229–354 | 同上 | f229–233 HOLD, f234–354 LOST_STOPPED (121 幀, 改動像素 = 0) | **Pass** |
| D. 重新回到畫面 | f150 / f355 | 延遲 ≤ 15 幀, IoU ≥ 0.8 | 延遲皆 0 幀 | **Pass** |
| E. UI 顯示 | L+2 / L+6 | 橘色實線 / 紅色虛線 +「遺失-未模糊」 | L+2: `#ff9f1a` 實線, 清單「遺失(保留模糊 3)」; L+6: `#ff4d4f` 虛線 `6 4`, 標籤「A 遺失-未模糊」, 清單「遺失-已停止模糊」; App 與無頭測試狀態一致 | **Pass** |
| F. 錯誤位置模糊 | 全片 | LOST_STOPPED 模糊面積 = 0 | 146 個 LOST_STOPPED 幀, 改動像素全部 = 0; 改動像素超出「外擴多邊形 + 羽化」= 0 | **Pass** |

- **狀態序列精確度**: hold = 0 / 5 / 15 三組, 所有遺失區段的狀態與 lost 計數**與預期完全一致, 誤差 0 幀**.
- **設計上的行為 (非錯誤, 但請知悉)**: HOLD 期間模糊停在「最後成功位置」. 在甩鏡與平移離開時, 這個位置與 A 的真實位置 IoU = 0, 也就是 **有 hold_frames 幀在錯誤位置模糊**. 遮擋情境 (A 沒動) 時 IoU ≥ 0.988.
- **是否需要修改**: 否.

---

## P0-3 分析快取 → 匯出是否避免重新追蹤

- **測試影片**: 合成 1280×720, 30 fps, 1500 幀, H.264 GOP 250 + 3 個 B 幀, 有音軌.
- **設定**: 第 300 幀框選, 第 900 幀 STOP.

### 實際流程 (已用計數驗證)

```
分析追蹤  analysis_range() = [300, 900)
  cap.set(POS_FRAMES, 300) → 讀 600 幀 → to_gray → process() → 寫入 t.cache (300..899)

匯出      從 frame 0 依序讀 1500 幀 (不 seek)
  每幀: to_gray → process():
          快取有 → 直接用, 不呼叫 track_step / recover
          範圍外 → INACTIVE, 不追蹤
        → render → 管線送 ffmpeg
```

| # | 測試 | 預期 | 實際 | 判定 |
|---|---|---|---|---|
| E1 | 分析後匯出, 計數追蹤呼叫 | track_step = recover = 0 | **track_step 0 次, recover 0 次**; 匯出期間快取完全沒變 | **Pass** |
| E1 | 重複計算量 (K5) | 只記錄 | `to_gray` 仍執行 1500 次, 共 3.75 s, 佔匯出時間 32.5 s 的 **11.5%** | 記錄 |
| E2 | 不分析直接匯出, 與 E1 比較 | poly 差 ≤ 0.5 px | 匯出時 track_step 599 次、recover 119 次 (即時補追蹤); 600 幀 poly **差異 0.0 px**; 兩支輸出 1499 幀**逐幀完全相同** | **Pass** |
| E3 | seek 對齊 (H.264 GOP250 + B 幀) | 200 個抽樣全部相同 | 0 / 200 不同 | **Pass** |
| E3 | seek 對齊 (HEVC 10-bit) | 同上 | 0 / 200 不同 | **Pass** |
| E3 | **seek 對齊 (可變幀率 30/60/24)** | 同上 | **165 / 200 不同** (從 k = 151 起就錯位, 可辨識的偏移為 −14 幀, 其餘偏移超過 ±15 幀); OpenCV 回報 fps 33.62 (實際為 30/60/24 混合) | **Fail** |
| E5 | 分析範圍 | 只處理 [300, 900) | 進度總數 600, 處理 f300–899, 快取 300..899 | **Pass** |
| E5 | 匯出時範圍外不模糊 | 範圍外 0 幀有模糊 | 範圍外 0 幀; 範圍內 0 幀缺少模糊位置 | **Pass** |
| E5 | `CAP_PROP_FRAME_COUNT` 準確度 (K4) | 只記錄 | 三支測試影片都與實際幀數相同 | 記錄 |

### E4 快取清除規則 (操作前快取皆為 300..899, 共 600 幀)

| 操作 | 預期 | 實際 | 判定 |
|---|---|---|---|
| hold_frames 5→9 / padding 5→3% / 強度 50→70 / 模式→馬賽克 / 模糊開關 | 不清除 | 600 幀全保留 | Pass |
| 在 f600 重新框選 | ≥600 清除 | 剩 300..600 (≥600 清除後, 立即重算當前幀 600) | Pass |
| 在 f700 STOP | ≥700 清除 | 剩 300..699 | Pass |
| 恢復 (移除 f700 STOP), 當前位置 f760 | ≥700 清除 | 300..699 (f760 顯示 UNTRACKED, 需重新分析) | Pass |
| min_conf 30→45 | 全部清除 | 0 | Pass |
| TR-960 → TR-1440 | 全部清除 + 重建樣板 | 0 | Pass |
| 儲存 → 載入專案 | 快取為空 | 0 | Pass |
| 刪除目標 | 目標移除 | 目標 0 個 | Pass |

- **VFR 錯位的影響**: 預覽 (`goto` 用 seek)、分析 (從起點 seek)、重建樣板 / 載入專案 (seek 到關鍵幀) 都會讀到**錯誤的幀**, 但快取以「幀號」存; 匯出從 0 依序讀, 讀到的是正確的幀. 結果: **匯出影片的模糊位置可能與預覽看到的差十幾幀以上**, 快速平移時會偏到窗戶外 (隱私外洩). iPhone 影片通常是可變幀率, 風險高.
- **是否需要修改**: 是 (見 🔧 #2, #4).

---

## P0-4 輸出影片: 幀數 / FPS / 音訊同步

- **方法**: 來源影片在同一時間點放「全白閃光幀」+「1 kHz 嗶聲」(每 5 秒一次), 匯出後量測閃光與嗶聲的時間差是否改變. 用 `ffprobe -count_frames` 計算實際幀數. 本項沒有框選目標 (只測輸出管線).
- **同步容許誤差**: 1 幀 + 23 ms.

| 來源 | 編碼器 | 解析度 | 幀率 | 幀數 | 音訊 | 同步 (最大漂移) | 匯出 fps | 判定 |
|---|---|---|---|---|---|---|---|---|
| S1 1080p 30 fps CFR, 60 s | x264 | ✓ | ✓ 30/1 | ✓ 1800 | ✓ | ✓ 0.1 ms | 87.8 | **Pass** |
| S1 | x265 | ✓ | ✓ | ✓ | ✓ | ✓ 0.1 ms | 75.3 | **Pass** |
| S1 | NVENC H.264 | ✓ | ✓ | ✓ | ✓ | ✓ 0.1 ms | 108.4 | **Pass** |
| S1 | NVENC H.265 | ✓ | ✓ | ✓ | ✓ | ✓ 0.1 ms | 109.1 | **Pass** |
| S2 1080p 29.97 fps CFR, 60 s | x264 | ✓ | ✗ 標記為 `979001/32666` (原片 `30000/1001`) | ✓ 1798 | ✓ | ✓ 0.1 ms | 86.9 | **Fail (輕微)** |
| **S3 1080p VFR 30/60/24, 60 s** | x264 | ✓ | ✗ 變成固定 38.01 fps | ✓ 2280 | ✓ | **✗ 最大 1.43 s** (−0.53 ~ +1.43 s) | 83.8 | **Fail** |
| S4 4K 60 fps CFR, 10 s | x264 | ✓ | ✓ 60/1 | ✓ 600 | ✓ | ✓ 0 ms | 19.8 | **Pass** |
| S4 | NVENC H.264 | ✓ | ✓ | ✓ | ✓ | ✓ 0 ms | 31.1 | **Pass** |
| S5 直式 | x264 | — | — | — | — | — | — | **未測試** (本機 ffmpeg 4.3 沒寫入旋轉資訊, 測試素材無效) |
| S6 HEVC 10-bit | x264 | ✓ | ✓ | ✓ 300 | ✓ | ✓ 0 ms | 68.2 | **Pass**; 10-bit → **8-bit** (`yuv420p10le` → `yuv420p`) 已確認. HDR 標記**未測試** (素材未寫入標記) |
| 取消匯出 | x264 | — | — | — | — | — | — | **Pass** (不殘留輸出檔) |

- **S2 原因**: `VideoSink` 用 `"-r", "%.6f" % fps` 傳入 29.970030 而不是 `30000/1001`. 60 秒內音畫漂移 0.1 ms, 1 小時約 3 ms, **實際影響很小**, 但剪輯軟體會看到非標準幀率.
- **S3 原因 (K2)**: OpenCV 只給一個平均 fps (38.01), 輸出以固定幀率重新排時間, 每一幀的時間點都改變了. 音軌照原時間, 所以 60 fps 段落畫面變慢、24 fps 段落變快, 漂移最大 1.43 秒.
- **是否需要修改**: 是 (見 🔧 #2, #3).

---

## P0-5 四點透視區域是否只模糊 polygon

- **方法**: 1920×1080 上 3 個強烈透視 / 旋轉的四邊形 (大、小、旋轉), padding 0 / 3 / 5%.
  - 黑色遮擋: 塗黑區域 vs 預期外擴多邊形
  - 高斯 / 馬賽克: 雜訊背景, 找所有被改動的像素
  - 高斯邊緣: 1 px 棋盤格反推實際混合權重 alpha, 檢查**真實窗戶範圍** (未外擴) 內是否模糊完整

| # | 條件 | 標準 | 實際 | 判定 |
|---|---|---|---|---|
| 1 | 四邊形而非外接矩形 | IoU ≥ 0.98 | 0.9934 ~ 0.9996 | **Pass** |
| 2 | 外接矩形內、多邊形外 | 改動像素 = 0 | 全部 0 (該區域 7,092 ~ 318,190 px 完全未動) | **Pass** |
| 3 | padding 5% 面積比 | 1.1025 ± 1% | 大: 1.106, 旋轉: 1.104 ✓; 小 (130×290 px): 1.120 (+1.6%) | **Pass** (小四邊形超出是像素量化: padding 0% 時已是 1.014) |
| 4 | 高斯 / 馬賽克柔邊範圍 | 只在「多邊形 + 羽化半徑」內 | 超出像素全部 0 | **Pass** |
| 5 | 透視變形跟隨 | CornerErr 中位數 ≤ 1%, 最大 ≤ 3% | V1-sim: 中位數 0.3%, 最大 1.5% | **Pass** |
| 6 | **高斯邊緣是否完整模糊窗戶** | 窗戶範圍內 alpha ≥ 0.9 | padding 5%: 最低 0.957 ✓; padding 3%: 小四邊形最低 **0.900** (剛好在邊界); **padding 0%: 最低 0.475, 13,446 ~ 14,595 px 的 alpha < 0.9** | padding ≥ 5% **Pass**; padding 0~3% **Fail** |

- **原因**: 羽化 (長邊 2% 的高斯) 以多邊形邊線為中心, 一半在內、一半在外. padding 太小時, 窗戶邊緣只有約 50~70% 的模糊強度.
- **影響**: 之前外部評論建議「外擴 2%」, 依本測試會讓窗戶邊緣模糊不完整. 目前預設 5% 是安全的.
- **是否需要修改**: 建議 (見 🔧 #5).

---

## 總結

### 🟢 已確認正常
- **P0-1 sim**: 窗戶本體外觀不同時, 4 種 min_conf 都 0 幀誤鎖; 找回延遲 0 幀; 角點誤差中位數 0.3%.
- **P0-2**: HOLD → LOST_STOPPED 轉換在 hold = 0 / 5 / 15 下**誤差 0 幀**; 146 個 LOST_STOPPED 幀**沒有任何一個像素被模糊**; 遮擋 50% 維持追蹤、遮擋 100% 不會追到遮擋物; UI 顏色 / 虛線 / 文字正確.
- **P0-3**: 分析後匯出**完全不重新追蹤** (track_step / recover 0 次); 分析 → 匯出與直接匯出結果逐幀完全相同; 快取清除規則 13 項全部符合預期; 分析範圍只處理 [300, 900).
- **P0-3 E3**: 固定幀率的 H.264 (長 GOP + B 幀)、HEVC 10-bit, seek 讀到的幀 100% 正確.
- **P0-4**: 固定幀率 30 / 60 fps (1080p、4K), x264 / x265 / NVENC 輸出的解析度、幀率、幀數完全一致, 有音訊, 音畫漂移 ≤ 0.1 ms; 取消匯出不殘留檔案.
- **P0-5**: 模糊區域確實是四點多邊形, 外接矩形內多邊形外 0 像素被改動; padding 5% 時窗戶邊緣完整模糊.

### 🟡 尚未確認 / 需要實機測試
- **M5 Mac / VideoToolbox**: 本機是 Windows, 完全沒測 (P1-1).
- **P0-4 直式影片 (旋轉資訊)**: 本機 ffmpeg 4.3 沒寫入旋轉資訊, 需要用 iPhone 直式影片重測.
- **P0-4 HDR 標記**: 同上, 需要 iPhone HDR 影片; 已確認 10-bit 會轉成 8-bit.
- **P0-1 實拍**: 只用合成影片; 真實窗戶的反光、窗簾不同、光線變化尚未驗證 (V2-real).
- **P0-3 E3 真實 iPhone 影片**: 合成 VFR 已確認會錯位, 需要確認 iPhone 實拍影片的錯位程度.

### 🔴 發現問題

| # | 項目 | 現象 | 重現 / 幀號 | 影響 |
|---|---|---|---|---|
| 1 | **P0-1 同型號窗戶誤鎖** | A 離開、外觀相同的 B 入鏡時, ORB 找回鎖到 B, conf 0.62~0.96, 顯示為正常追蹤中 (綠框) | V1-twin f80–253 (174 幀); min_conf 0.7 仍 173 幀 | 錯誤模糊 B 約 5.8 秒, 且 UI 沒有任何警示; A 回來後會正確切回 |
| 2 | **P0-3 可變幀率 seek 錯位** | `cap.set(POS_FRAMES)` 在 VFR 影片跳到錯誤的幀 | 合成 VFR 30/60/24, 165/200 抽樣錯位, 偏移 ≥ 14 幀 | 預覽 / 分析 / 關鍵幀樣板讀到的畫面與匯出不同 → **匯出模糊位置偏移, 可能隱私外洩** |
| 3 | **P0-4 可變幀率音畫不同步** | 輸出變成固定 38.01 fps, 每幀時間重排 | S3, 最大漂移 1.43 s | iPhone 影片匯出後聲音對不上 |
| 4 | P0-4 29.97 fps 幀率標記不標準 | `30000/1001` 變成 `979001/32666` | S2 | 影響極小 (1 小時約 3 ms), 剪輯軟體顯示非標準幀率 |
| 5 | P0-5 padding 過小時邊緣模糊不完整 | 羽化一半在窗戶內 | padding 0%: alpha 最低 0.475; 3%: 0.900 | 使用者把外擴調到 < 5% 時窗戶邊緣半透明 |

### 🔧 建議修改 (本輪未修改)

| 優先 | 問題 | 建議改法 | 是否動到追蹤核心 |
|---|---|---|---|
| **P0** | #2 VFR seek 錯位 | 所有需要特定幀的地方 (預覽跳轉、分析起點、重建樣板、載入專案) 改用「從最近的已知位置依序讀」或以 `CAP_PROP_POS_MSEC` 時間戳對齊; 最小改法: 開啟影片時偵測 VFR (ffprobe `r_frame_rate` ≠ `avg_frame_rate`), 若是 VFR 則分析一律從 0 依序讀並提示使用者 | 否 (只改讀影片) |
| **P0** | #3 VFR 音畫不同步 | 匯出時保留原始時間戳: 以 ffprobe 取得每幀 pts, 寫成 timecode 檔讓 ffmpeg 使用 (或 `-vsync passthrough` 搭配原始時間戳); 最小改法: 偵測到 VFR 時警告, 並提供「先用 ffmpeg 轉成固定幀率」的選項 | 否 |
| **P0** | #1 同型號窗戶誤鎖 | 不改 KLT/ORB/NCC 本身, 在其上加限制: (a) **找回時的位置合理性**: 遺失期間累計 KLT 背景運動 (或相機運動) 估計 A 的預期位置, 找回位置離預期太遠就拒絕; (b) 遺失超過 N 幀後, 找回改為「需使用者確認」或 UI 顯示「重新找回 (請確認)」黃框; (c) 樣板外擴從 25% 加大 (讓周圍牆面佔更多比重) — 需重測 sim / twin | (a)(b) 否, 加在外層; (c) 動到樣板參數 |
| P1 | #4 29.97 fps 標記 | `-r` 改用 ffprobe 讀到的原始有理數 `r_frame_rate` | 否 |
| P1 | #5 padding 過小 | 羽化改為只往外 (先把遮罩擴張羽化半徑再模糊), 或 UI 外擴下限設為 5% | 否 |
| P2 | 匯出重複 to_gray (11.5% 時間) | 快取已涵蓋、或目標 INACTIVE 的幀跳過 `to_gray` | 否 |
