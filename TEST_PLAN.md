# video-blur 測試計畫 (驗證現況, 不改演算法)

對象版本: `main` @ `2fdb4f0`
主要環境: Apple Silicon M5 Mac (VideoToolbox 相關項目**必須**在 M5 實機執行, 不得引用 Windows 結果)

## 0. 執行規則

1. 本輪以**測試與回報**為主. 不修改 `tracker.py` 的 KLT + Homography + ORB/NCC 核心.
2. 可以新增測試腳本 (建議放 `tests/`), 可以用 monkeypatch 計數或記錄, 但不改變被測程式行為.
3. 發現問題: 先回報「實際流程 + frame number + 建議修改方向」, **不要直接修**.
4. 每項測試結果只能是 `Pass` / `Fail` / `未測試`; 標為「目標值」的指標未達標記 🟡, 不算 Fail.
5. 每次測試記錄: macOS 版本、機型/記憶體、Python 版本、OpenCV 版本、ffmpeg 版本與來源 (`brew` 等)、`ffmpeg -hide_banner -encoders | grep videotoolbox` 輸出.

---

## 1. 統一名詞

### 1.1 追蹤解析度 (Tracking Resolution, 簡稱 TR)

| 測試代號 | UI 顯示 | 程式 `engine.work_width` | 專案 JSON `settings.work_width` | 實際追蹤影像寬度 |
|---|---|---|---|---|
| **TR-960** | `960 (快)` | `960` | `960` | `min(960, 原片寬)` |
| **TR-1440** | `1440` | `1440` | `1440` | `min(1440, 原片寬)` |
| **TR-1920** | `1920` | `1920` | `1920` | `min(1920, 原片寬)` |
| **TR-Native** | `原始解析度 (慢)` | `0` | `0` | 原片寬 |

- 不會放大: 原片寬 ≤ TR 時等同 TR-Native (例如 1280 寬影片的 TR-1440 = TR-Native).
- TR **只影響追蹤運算**, 輸出影片一律為原解析度.
- 報告中一律使用「測試代號」, 不要混用 960 / 1440p / 原始 / Original 等寫法.

### 1.2 目標狀態 (以程式狀態字串判定)

| 狀態代號 | 狀態字串 (`results[i][2]`) | 是否模糊 | 畫面框線 |
|---|---|---|---|
| **TRACKING** | `追蹤中 NN%` | 是, 在追蹤位置 | 綠色實線, 標籤含可信度 |
| **HOLD** | `遺失(保留模糊 n)` (n = 1..hold_frames) | 是, 在最後成功位置 (anchor) | 橘色實線 |
| **LOST_STOPPED** | `遺失-已停止模糊` | **否** | 紅色虛線 `#ff4d4f`, 標籤「遺失-未模糊」 |
| **INACTIVE** | `未啟用` | 否 | 無 (在 STOP 之後或第一個框選之前) |
| **UNTRACKED** | `尚未追蹤到此幀` | 否 | 無 (尚未分析到該幀) |

### 1.3 測量指標 (需要 GT 真值; 合成影片由程式給, 實拍影片需人工標四角, 至少每 10 幀一次)

令 `M_blur` = 該幀實際模糊的多邊形 `expand_poly(poly, padding)` 的遮罩, `G_A` / `G_B` = 窗戶 A / B 的真值遮罩 (裁切到畫面內).

| 指標 | 定義 |
|---|---|
| **可見率 Vis_A** | A 在畫面內的面積 / A 完整面積 |
| **漏遮率 Leak_A** | \|G_A ∧ ¬M_blur\| / \|G_A\| (Vis_A ≥ 2% 才計算) |
| **誤遮 B 率 Wrong_B** | \|M_blur ∧ G_B\| / \|G_B\| |
| **誤鎖 FalseLock** | 狀態為 TRACKING 或 HOLD, 且 Wrong_B ≥ 0.3 且 IoU(M_blur, G_A) < 0.3 |
| **角點誤差 CornerErr** | 四角最大誤差 / A 對角線長 (只在 Vis_A = 100% 時計算) |
| **找回延遲 RecoverLag** | 從 Vis_A 首次 ≥ 50% 起, 到 TRACKING 且 IoU(poly, G_A) ≥ 0.8 的幀數 |

每支測試請輸出逐幀 CSV:
`frame, state, conf, lost, poly(8 值), vis_A, leak_A, wrong_B, iou_A, corner_err`

### 1.4 測試影片編號

| 代號 | 內容 | 規格 | 用途 |
|---|---|---|---|
| **V1-sim** | 合成: 兩扇窗, 窗框相同、窗內內容不同, 周圍紋理不同 | 1920×1080, 30 fps, ≥ 20 s | P0-1 主測 (有 GT) |
| **V1-ident** | 合成: 兩扇窗與周圍 25% 範圍像素完全相同 | 同上 | P0-1 已知極限, 只記錄 |
| **V2-real** | 實拍: 同一面牆上至少兩扇相似窗戶, 快速平移讓 A 離開、B 入鏡、再回到 A | 4K, 30 或 60 fps, ≥ 30 s | P0-1 實拍驗證 (人工標 GT) |
| **V3-4K60** | 實拍 vlog 風格, 相機持續移動 | 3840×2160, 60 fps, ≥ 3 分鐘, 有音軌 | P1-4, P2-8 |
| **V4-iPhone** | iPhone 直接拍攝 (HEVC, 10-bit HDR, 可變幀率, 直式) | 4K, 30/60 fps, ≥ 1 分鐘, 有音軌 | P0-3 對齊、P1-4 相容性 |
| **V5-oblique** | 合成: 純色背景上一個強烈透視的斜四邊形 | 1920×1080 | P0-5 模糊區域 |
| **V6-occlude** | 合成: A 被遮擋 3 幀 (遮 50%)、30 幀 (遮 100%)、離開畫面 120 幀後回來 | 1920×1080, 30 fps | P0-2 |
| **V7-long** | 合成長片 (已知 homography): 相機緩慢來回, A 全程可見 | 3840×2160, 30 fps, 5 分鐘 | P2-8 漂移 |

合成影片請用固定亂數種子, 並一併輸出每幀 GT 四角 (JSON/CSV).

---

## 2. 優先級總覽

| 優先 | 編號 | 項目 | 原需求編號 |
|---|---|---|---|
| **P0** | P0-1 | 相似窗戶誤追蹤 | 1 |
| **P0** | P0-2 | 遺失 / 保留 / 停止模糊 | 5 |
| **P0** | P0-3 | 分析結果快取與匯出行為 (含 seek 對齊) | 6 + 7 |
| **P0** | P0-4 | 輸出正確性 (幀數、解析度、FPS、音訊同步) | 2 的正確性部分 |
| **P0** | P0-5 | 模糊區域幾何正確性 | 8 |
| **P1** | P1-1 | M5 VideoToolbox 效能與畫質 | 2 的效能部分 |
| **P1** | P1-2 | 追蹤解析度切換 | 4 |
| **P1** | P1-3 | 專案儲存 / 載入 | 9 |
| **P2** | P2-1 | 4K 長片效能、記憶體、漂移 | 3 |

P0 = 會造成**隱私外洩 (該糊沒糊)** 或**輸出錯誤**; P1 = 功能正確但影響使用; P2 = 效能與長時間穩定性.

---

## 3. 需求中需要更正的前提 (已對照程式碼)

測試前請知悉, 以免測錯方向:

1. **「匯出會從 frame 0 重新追蹤整部影片」不正確.**
   匯出確實從 frame 0 **讀取並解碼**每一幀, 但 `engine.process()` 會先查 `t.cache[idx]`, 已分析過的幀**直接使用快取, 不呼叫 `track_step()` / `recover()`**. 只有快取中沒有的幀才會追蹤. P0-3 用計數驗證這點.
2. **「recovery threshold 提高到 0.5」不正確.**
   找回時 NCC > 0.5 從第一版就存在. 第二版的改動是:
   (a) 找回門檻改為 `max(0.5, min_conf)`;
   (b) **新增** KLT 逐幀追蹤結果也要通過 `min_conf` (預設 0.3), 否則視為遺失.
   所以 P0-1 應比較 `min_conf` = 0 / 0.3 / 0.5 / 0.7, 而不是「0.5 前後」.

---

## 4. 程式碼走讀已知的風險點 (需實測確認, 不要先修)

| 編號 | 風險 | 位置 | 對應測試 |
|---|---|---|---|
| K1 | 「分析追蹤」用 `cap.set(POS_FRAMES, start)` 跳到起點; 預覽也用 seek; 匯出則從 0 依序讀. 若該編碼格式 seek 不精準, 快取的幀號與匯出時的實際畫面會錯開 → 模糊位置偏移 | `app.py` `_batch_worker`, `_read`, `_rebuild_refs`, `load_project` | P0-3 E3 |
| K2 | 輸出用 `CAP_PROP_FPS` 當固定幀率 (`-r`), 可變幀率來源 (iPhone) 可能音畫逐漸不同步 | `VideoSink` | P0-4 |
| K3 | OpenCV 只解碼 8-bit, 10-bit HDR 來源輸出會變 8-bit SDR, 顏色可能偏移 | `cv2.VideoCapture` | P0-4 |
| K4 | `n_frames` 取自 `CAP_PROP_FRAME_COUNT`, 可能不準, 影響 `analysis_range()` 終點與進度條 | `open_video`, `analysis_range` | P0-3 E5 |
| K5 | 匯出時已快取的幀仍會做 `to_gray()` (轉灰階 + 縮放), 屬多餘計算 | `_batch_worker` | P0-3 E1 (量化) |
| K6 | 每個關鍵幀的樣板保存整張追蹤尺寸灰階圖 + 遮罩; TR-Native 4K 每個關鍵幀約 16 MB | `build_ref` | P2-1 |
| K7 | 4K 原始畫面以 bgr24 經管線送 ffmpeg, 60 fps 約 1.5 GB/s, 可能是端到端瓶頸 (非編碼器) | `VideoSink.write` | P1-1 |

---

## 5. 測試項目

### P0-1 相似窗戶誤追蹤 【最重要】

**影片**: V1-sim (主), V1-ident (極限), V2-real (實拍)
**參數**: TR-960, hold_frames = 5, padding = 5%; `min_conf` 依序 0 / 0.3 / 0.5 / 0.7 各跑一次

**操作**
1. 第 0 幀以四點框選 A.
2. 「分析追蹤」全片 (或無頭腳本直接呼叫 `_batch_worker(src, None, state)`).
3. 輸出逐幀 CSV, 並記錄以下事件的幀號: A 開始離開 (Vis_A < 100%)、A 完全離開、B 入鏡、B 完全可見、A 回來 (Vis_A ≥ 50%)、TRACKING 恢復.

**通過標準** (`min_conf = 0.3`, V1-sim)

| # | 條件 | 標準 |
|---|---|---|
| 1 | 誤鎖 | FalseLock 幀數 = **0** |
| 2 | 誤鎖時仍模糊 | 若有誤鎖幀: 列出所有幀號與 conf, 直接判 Fail |
| 3 | 可信度區分能力 | 追蹤正確 (IoU_A ≥ 0.8) 幀的 conf 第 5 百分位, 要高於所有誤鎖候選幀的 conf; 回報兩組分布 (min / p5 / median) |
| 4 | 找回 | RecoverLag ≤ **15 幀** |
| 5 | 找回後正確 | 找回後 30 幀內: 每幀 Leak_A ≤ 5%, IoU_A ≥ 0.8 |
| 6 | 狀態顯示 | A 完全離開後依序出現 HOLD(1..5) → LOST_STOPPED; 狀態與框線顏色符合 §1.2 |

**其他參數與影片**: `min_conf` = 0 / 0.5 / 0.7 以及 V1-ident 只記錄結果, 不判 Pass/Fail. 回報各 `min_conf` 的 FalseLock 幀數、RecoverLag, 以及 A 可見時的誤判遺失幀數 (A 可見卻非 TRACKING), 用來找最佳預設值.
**V2-real**: 用同樣標準, 但 GT 為每 10 幀人工標註.

---

### P0-2 遺失 / 保留 / 停止模糊

**影片**: V6-occlude; 補充 V2-real 的快速平移段
**參數**: hold_frames = 5 (另跑 0 與 15 做對照), min_conf = 0.3

| 情境 | 操作 | 通過標準 |
|---|---|---|
| A. 短暫離開畫面 (< 5 幀) | V6 或 V2 | 離開期間狀態為 HOLD, 且 HOLD 期間持續模糊; 回來後 ≤ 15 幀恢復 TRACKING |
| B. 遮擋 50%, 3 幀 | V6 | 狀態維持 TRACKING 或 HOLD; 未被遮住的 A 可見部分 Leak ≤ 5% |
| C. 遮擋 100% / 長時間消失 (≥ 30 幀) | V6 | 設第一個非 TRACKING 幀為 L: 幀 L..L+4 為 HOLD 並模糊 anchor; **幀 L+5 起 LOST_STOPPED 且不模糊** (容許誤差 **0 幀**) |
| D. 重新回到畫面 | V6 | RecoverLag ≤ 15 幀; 恢復後 IoU_A ≥ 0.8 |
| E. UI 顯示 | 在 L+2 與 L+6 暫停截圖 | L+2 為橘色實線; L+6 為紅色虛線 + 「遺失-未模糊」; 目標清單狀態文字一致 |
| F. 錯誤位置模糊 | 全片 | LOST_STOPPED 幀的 M_blur 面積 = 0; HOLD 幀記錄 anchor 與真值的 IoU (設計上允許偏差, 只記錄) |

---

### P0-3 分析結果快取與匯出行為

**先確認程式流程**, 報告需附上下列事實 (以實際執行驗證, 不只是讀程式碼):

```
分析追蹤  run_batch(None) → _batch_worker(out_path=None)
  (start, end) = analysis_range()      # 最早框選幀 ~ 最後 STOP 幀 (沒有 STOP 則到 n_frames)
  cap.set(POS_FRAMES, start)           # seek
  for idx in [start, end): read → to_gray → engine.process() → 寫入 t.cache[idx]

匯出      run_batch(out) → _batch_worker(out_path=out)
  從 frame 0 依序讀到片尾 (不 seek)
  for idx: read → to_gray → engine.process():
      t.cache 有 idx → 直接用 (不追蹤)
      沒有 → 用 idx-1 的快取 + 前一幀灰階追蹤, 並寫入快取
  → render 模糊 → 管線送 ffmpeg
```

**快取清除規則** (E4 需逐項驗證):

| 操作 | 預期對快取的影響 |
|---|---|
| 新增 / 重新框選 (幀 k) | 該目標 `cache` 中 ≥ k 的幀清除 |
| 從此幀取消 STOP (幀 k) | 該目標 ≥ k 清除 |
| 恢復追蹤 (移除 STOP k) | 該目標 ≥ k 清除 |
| 變更 `min_conf` | **所有目標**快取全部清除 |
| 變更 TR | 所有目標快取清除 + 樣板重建 |
| 變更 hold_frames / padding / 強度 / 模糊方式 / 模糊開關 | **不清除** (這些在查詢或繪製時才套用) |
| 刪除目標 | 該目標整個移除 |
| 載入專案 | 快取為空 (未分析幀顯示 UNTRACKED) |

**測試**

| # | 操作 | 通過標準 |
|---|---|---|
| E1 | 分析完成後匯出; 以 monkeypatch 計數 `tracker.track_step` 與 `tracker.recover` 的呼叫次數 | 匯出期間兩者呼叫次數 = **0**; 另回報匯出時 `to_gray` 所花的總時間 (K5) |
| E2 | 同一專案: (a) 分析→匯出 (b) 不分析直接匯出, 比較每幀 poly | 每幀四角差異 ≤ **0.5 px**; 兩支輸出影片逐幀 PSNR = ∞ 或 ≥ 60 dB |
| E3 | **seek 對齊** (K1): V3-4K60、V4-iPhone 各隨機抽 200 個幀號 k, 比較 `seek(k)` 讀到的畫面與從 0 依序讀到第 k 幀 | 所有 k 的畫面 MD5 **完全相同**; 不同則列出 k 與相差幾幀, 判 **Fail** |
| E4 | 逐項執行上表操作, 記錄操作前後 `len(t.cache)` 與最小 / 最大快取幀號 | 與上表完全一致 |
| E5 | 在第 300 幀框選、第 900 幀 STOP, 執行分析 | 只處理 [300, 900) (進度總數 600); 範圍外沒有快取; 匯出時 < 300 與 ≥ 900 的幀都不模糊; 另回報 `CAP_PROP_FRAME_COUNT` 與實際可讀幀數 (K4) |

若 E3 Fail: 回報實際流程、受影響的功能 (預覽、分析、重建樣板、載入專案), 以及建議改法 (例如分析改成從 0 依序讀但跳過追蹤, 或改用時間戳 `CAP_PROP_POS_MSEC` 對齊), **先不要修**.

---

### P0-4 輸出正確性

**影片**: V3-4K60、V4-iPhone; 每個可用編碼器都跑一次
**檢查方式**: `ffprobe -v error -show_streams -count_frames` 比較來源與輸出

| # | 條件 | 通過標準 |
|---|---|---|
| 1 | 解析度 | 寬高與來源完全相同 (直式影片方向也要正確) |
| 2 | 幀率 | `r_frame_rate` 與來源相同 (可變幀率來源: 記錄 `avg_frame_rate`) |
| 3 | 幀數 | `nb_read_frames` 與來源相同, 差 **0** |
| 4 | 音訊 | 來源有音軌 → 輸出必須有 AAC 音軌 |
| 5 | 音畫同步 | \|視訊長度 − 音訊長度\| ≤ 1 幀 + 23 ms; 另在片尾找一個拍手或清楚的聲音事件人工比對, 偏差 ≤ 1 幀 |
| 6 | HDR (K3) | V4 輸出記錄 `color_transfer` / `pix_fmt`, 並截圖比對顏色 (只記錄) |
| 7 | 穩定性 | 無 crash, 無 ffmpeg 錯誤, 取消匯出後不殘留輸出檔 |

---

### P0-5 模糊區域幾何正確性

**影片**: V5-oblique (純色背景, 強烈透視四邊形); 以及 P0-1 追蹤中的幀
**參數**: 模糊方式設為「黑色遮擋」(遮罩邊緣最精確), 另跑「高斯模糊」檢查柔邊

| # | 條件 | 通過標準 |
|---|---|---|
| 1 | 形狀為四邊形而非外接矩形 | 被塗黑的像素與 `expand_poly(poly, 0.05)` 的 IoU ≥ **0.98** |
| 2 | 外接矩形內、多邊形外的區域 | 將預期多邊形擴張 2 px 後, 外面被改動的像素數 = **0** |
| 3 | padding 5% | 模糊面積 / 原四邊形面積 = 1.1025 ± 1% (以頂點平均為中心、線性放大 5%) |
| 4 | 高斯柔邊 | 被改動像素只出現在「預期多邊形 + 羽化半徑 (長邊的 2%)」內 |
| 5 | 透視變形跟隨 | P0-1 中 Vis_A = 100% 的 TRACKING 幀: CornerErr 中位數 ≤ 1%, 最大值 ≤ 3% |

---

### P1-1 M5 VideoToolbox 效能與畫質

**環境**: M5 實機. 先確認 `ffmpeg -encoders` 有 `h264_videotoolbox` / `hevc_videotoolbox`, 沒有則整項標為「未測試」並說明 ffmpeg 來源.
**影片**: V3-4K60 (3 分鐘); 輸出正確性套用 P0-4 標準.
**編碼器**: H.264 Apple 硬體 / H.265 Apple 硬體 / H.264 x264 / H.265 x265

每個編碼器量**兩種**, 以區分瓶頸 (K7):

- **(a) 單純編碼**: `ffmpeg -i V3 -an <與 video_args() 相同的參數> out.mp4` (不經過 Python)
- **(b) 端到端**: App 匯出, 條件為 TR-960、1 個目標、已先分析

| 記錄項目 | 方法 |
|---|---|
| 耗時、平均 fps | 牆鐘時間 / 幀數 |
| CPU 使用率 | `ps -o %cpu` 每秒取樣 python 與 ffmpeg (平均與峰值); 可選 `sudo powermetrics --samplers cpu_power,gpu_power` |
| 峰值記憶體 | `/usr/bin/time -l` 的 maximum resident set size |
| 檔案大小、位元率 | `ffprobe` |
| 畫質 | 在模糊區域外, 以 SSIM 與原片比較 (`ffmpeg -lavfi ssim`, 或 Python 遮罩計算); 有 libvmaf 時一併記錄 VMAF |

**通過標準**

| # | 條件 | 標準 |
|---|---|---|
| 1 | VideoToolbox 可用 | 兩個 Apple 硬體編碼器都能成功輸出, 且通過 P0-4 第 1~5 項 |
| 2 | 真的有用到硬體 | ffmpeg 記錄與 `ffprobe` 編碼器資訊相符; (a) 的 ffmpeg CPU% ≤ x264 的 **50%** |
| 3 | 速度 (目標值) | (a) 硬體編碼 fps ≥ x264 medium 的 **2 倍** |
| 4 | 畫質 | x264 / x265: SSIM ≥ **0.97**; Apple 硬體: SSIM ≥ **0.95** (目標值) |
| 5 | 瓶頸判定 | 若 (b) fps 明顯低於 (a), 報告瓶頸在 Python 端 (解碼 / 追蹤 / 繪製 / 管線), 並附各階段耗時 |

**長片穩定性**: 用 1080p 30 分鐘 (或 4K 10 分鐘) 以 H.264 Apple 硬體匯出一次, 套用 P0-4 第 3、5、7 項.

---

### P1-2 追蹤解析度切換

**影片**: V3-4K60 (寬 3840, 使四種 TR 確實不同)

| # | 操作 | 通過標準 |
|---|---|---|
| 1 | UI 依序切換 TR-960 → TR-1440 → TR-1920 → TR-Native → TR-960 | 每次 `engine.work_width` 為 960 / 1440 / 1920 / 0 / 960 |
| 2 | 樣板重建 | 每個關鍵幀 `t.refs[k]["gray"].shape[1]` = min(TR, 3840); `refs[k]["poly"]` = 原圖座標四角 × 縮放比例 (誤差 ≤ 1e-3) |
| 3 | 四點不跑位 | 切換前後 `t.keyframes[k]` 完全相同 (位元相等); 畫面框線位置不變 (截圖比對) |
| 4 | 目標不消失 | 目標數、名稱、模糊開關、STOP 關鍵幀不變 |
| 5 | 快取 | 切換後 `len(t.cache)` = 0, 當前幀狀態為 TRACKING (位於關鍵幀) 或 UNTRACKED |
| 6 | 切換後可追蹤 | 從關鍵幀播放或分析 300 幀: 無例外; 與切換前同段落的 poly 相比, CornerErr 中位數 ≤ 1% |
| 7 | 各 TR 追蹤品質 | 同一段落在四種 TR 下的 Leak_A / FalseLock (如有 GT) 與追蹤 fps, 只記錄 |
| 8 | 專案 | 各 TR 下存檔 → 重開程式 → 載入: `work_width` 與 UI 顯示恢復, 樣板以恢復後的 TR 建立 |

---

### P1-3 專案儲存 / 載入

**建立專案內容**: 3 個目標
- 目標 1: 矩形, 模糊開, 關鍵幀 0 → STOP 120 → 重新框選 200
- 目標 2: 四點, 模糊**關**
- 目標 3: 四點, 關鍵幀 50 → STOP 400

**設定**: 馬賽克、強度 70、外擴 3%、hold_frames 10、min_conf 45%、TR-1440

**操作**: 儲存 → **結束 Python 行程** → 重新啟動 → 載入

**通過標準**

| 類別 | 欄位 | 標準 |
|---|---|---|
| 會保存 | 影片路徑 | 恢復 (影片移到專案同資料夾時也能找到) |
| 會保存 | mode / strength / padding / hold / min_conf / work_width | 與儲存前完全相同 (padding、min_conf 以整數 % 比對), UI 控制項顯示一致 |
| 會保存 | 目標名稱、數量、順序、模糊開關 | 完全相同 |
| 會保存 | 關鍵幀幀號、STOP、四角座標 | 幀號與 STOP 完全相同; 座標誤差 ≤ 1e-3 px |
| 會保存 | 樣板 | 以 TR-1440 重建 (`refs[k]["gray"].shape[1]` = 1440) |
| 設計上不保存 | 輸出編碼、顯示框線、預覽模糊、追蹤快取 | 回到預設值; 快取為空 (需重新分析) — 符合即 Pass |
| 行為 | 載入後分析 → 匯出 | 與儲存前在同一行程內直接匯出的影片逐幀 PSNR ≥ 60 dB |

---

### P2-1 4K 長片效能、記憶體、漂移

**影片**: V3-4K60 (≥ 3 分鐘), V7-long (5 分鐘, 有 GT)
**參數**: 1 個目標; TR-960 / TR-1440 / TR-1920 / TR-Native 各跑一次「分析追蹤」, 再以 TR-960 匯出一次

| 記錄 | 方法 |
|---|---|
| 分析 fps | 幀數 / 牆鐘時間; 另外分開計時: 解碼、to_gray、追蹤 (`process`) |
| CPU% | 每秒取樣 python 行程 |
| RSS 曲線 | 每 10 秒取樣 python 行程 RSS, 輸出 CSV |
| 漂移 | V7-long 每 30 秒計算一次 CornerErr |

**通過標準**

| # | 條件 | 標準 |
|---|---|---|
| 1 | 穩定性 | 四種 TR 都無 crash, 無例外 |
| 2 | 記憶體 | 開始後 1 分鐘起算, RSS 增長 ≤ **50 MB/分鐘** (快取每幀每目標僅數百 bytes); 超過則回報增長曲線 |
| 3 | 漂移 | V7-long 全片 CornerErr 中位數 ≤ **2%**, 最大值 ≤ **5%**, 且最後 1 分鐘的中位數不大於第 1 分鐘的 2 倍 |
| 4 | 匯出 | 完成並通過 P0-4 第 1~5 項 |
| 5 | 速度 (目標值) | 4K60 下 TR-960 分析 fps ≥ 30; 其餘 TR 只記錄 |
| 6 | 預覽播放 | 4K60 連續播放 5 分鐘無 crash, RSS 增長 ≤ 50 MB/分鐘 (只記錄 UI 實際播放 fps) |

---

## 6. 每項測試回報格式

```markdown
### <編號> <測試項目>
- 測試環境: 機型 / macOS / Python / OpenCV / ffmpeg (來源) / 記憶體
- 測試影片: <代號> — 解析度, fps (CFR/VFR), 長度, 編碼, 是否有音軌, 是否 HDR
- 參數: TR-xxx, min_conf, hold_frames, padding, 編碼器
- 操作方式: <步驟, 或腳本路徑與指令>
- 預期結果: <引用本文件的通過標準>
- 實際結果: <數值; 附 CSV / 截圖路徑>
- 判定: Pass / Fail / 未測試 (未測試需說明原因)
- 失敗幀號: <frame number 與當時狀態、conf>
- 效能數據: fps / CPU% (平均, 峰值) / RSS (峰值, 增長率)
- 是否需要修改程式: 是 / 否
- 建議修改方向: <不要直接修改>
```

## 7. 總結報告格式

```markdown
## 🟢 已確認正常
- <編號>: 一句話結論 + 關鍵數據

## 🟡 尚未確認 / 需要實機測試
- <編號>: 缺什麼 (影片、硬體、工具) 與原因

## 🔴 發現問題
- <編號>: 現象, 重現步驟, 幀號, 影響 (隱私外洩 / 輸出錯誤 / 效能)

## 🔧 建議修改
| 優先 | 問題 | 建議改法 | 是否會動到追蹤核心 |
```
